<UserControl x:Class="ImoutoRebirth.Tori.UI.Steps.WelcomeStepControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImoutoRebirth.Tori.UI.Steps"
             xmlns:steps="clr-namespace:ImoutoRebirth.Tori.UI.ViewModels.Steps"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=steps:WelcomeStepViewModel}"
             FontFamily="Roboto">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="50, 40, 50, 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding Title}" FontSize="41" FontWeight="Regular" />

            <Image Grid.Column="1" />
        </Grid>
        
        <Grid Grid.Row="1" Margin="144, 43, 144, 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" FontSize="16" VerticalAlignment="Top">
                <Run Text="New update available" />
                <LineBreak />
                <Run Text="Current version" />
                <Run Text="{Binding CurrentVersion}" />
                <LineBreak />
                <Run Text="New version" />
                <Run Text="{Binding NewVersion}" />
            </TextBlock>

            <TextBlock Grid.Column="1" TextWrapping="Wrap" FontSize="16" VerticalAlignment="Top">
                <Run Text="Click the update button for the instant update. Or click customize in case you want to change some settings." />
                <LineBreak />
                <LineBreak />
                <LineBreak />
                <Run FontSize="12" Text="Note: ImoutoRebirth background services will be stopped and all instances of ImoutoNavigator and ImoutoViewer will be closed too. Please save your work before continue" />
            </TextBlock>
        </Grid>

        <StackPanel Grid.Row="2" HorizontalAlignment="Right" Orientation="Horizontal" Margin="50, 0, 50, 40">
            <Button Width="168" Height="40">customize</Button>
            <Button Width="168" Height="40" Margin="40,0,0,0">update</Button>
        </StackPanel>
    </Grid>
</UserControl>
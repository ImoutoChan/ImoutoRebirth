<UserControl x:Class="ImoutoRebirth.Tori.UI.Steps.Installation.InstallationStepControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImoutoRebirth.Tori.UI.Steps.Installation"
             xmlns:userControls="clr-namespace:ImoutoRebirth.Tori.UI.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="614" d:DesignWidth="1028"
             d:DataContext="{d:DesignInstance Type=local:InstallationStepViewModel}"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Margin="144, 73, 144, 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <mah:MetroProgressBar Value="34" />

            <TextBox
                x:Name="LogTextBox"
                IsReadOnly="True"
                Margin="0,41,0,0"
                Grid.Row="1"
                Text="{Binding LogString}"
                TextChanged="TextBoxBase_OnTextChanged"/>
        </Grid>

        <StackPanel Grid.Row="2" HorizontalAlignment="Right" Orientation="Horizontal" Margin="50, 40, 50, 40">
            <Button
                Style="{DynamicResource Styles.Button.Regular}"
                Command="{Binding GoBackCommand}"
                Visibility="{Binding IsInstallationStarted, Converter={StaticResource ConditionVisibilityConverter}, ConverterParameter=false}"
                Content="back" />
            <Button
                Style="{DynamicResource Styles.Button.Accent}"
                Command="{Binding InstallCommand}"
                Content="install"
                Visibility="{Binding IsInstallationFinished, Converter={StaticResource ConditionVisibilityConverter}, ConverterParameter=false}"
                IsEnabled="{Binding IsInstalling, Converter={StaticResource ConditionVisibilityConverter}, ConverterParameter=false}" />
            <Button
                Style="{DynamicResource Styles.Button.Accent}"
                Command="{Binding CloseAppCommand}"
                Content="ok"
                Visibility="{Binding IsInstallationFinished, Converter={StaticResource ConditionVisibilityConverter}, ConverterParameter=true}" />
        </StackPanel>
    </Grid>
</UserControl>
